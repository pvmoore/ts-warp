package warp.actions;

import warp.event.EventLoop;
import warp.parse.Tokens;

import java.io.File;

final public class LexAction {

    public LexAction(EventLoop events) {

    }
    public void run(File file) {
        var tokens = new Tokens();


        //events.fire()
    }
}
